# 這個資料夾在做什麼

日本免費插圖庫「いらすとや」的查詢與排版工具。約 2.5 萬張，全部可商用。
使用者是助人工作者（心理師、社工、行政人員），要拿這些圖做衛教單、學習單、簡報。

這份是寫給 AI 看的。使用者只會用日常說法問問題，底下的規矩請自己遵守。

---

## 🔴 三條硬規矩

### ① 圖要進 Artifact，一定要先跑 `inline_images()`

**沒跑就發布 → 圖全部是破的，而且不會報錯。**

Artifact 有嚴格的 CSP，會擋掉所有外部主機的圖片（唯一例外是 Google Fonts）。
`blogger.googleusercontent.com` 的圖網址在 Artifact 裡一律載不出來。

正確順序是：

```
1. search(...) 拿到圖
2. 寫成 HTML 檔
3. inline_images(html_path)   ← 把圖就地換成內嵌的 data: URI
4. 這時才發布
```

漏掉第 3 步的症狀特別惡劣：**HTML 看起來完全正常、發布也成功、沒有任何錯誤訊息**，
只有使用者打開頁面時看到一排破圖。你不會知道，他會以為是自己弄壞的。

### ② 授權：同一件作品最多 20 張

原文與翻譯都放在這裡，不要憑印象轉述：

> 商用目的の場合、一つの作成物の中に **20点まで** は無料でご利用いただけます。
> **【繁中】** 商業用途時，同一件作品中最多可免費使用 **20 張**；超過的部分每張 1,100 日圓。

> 素材そのものの販売・再配布は禁止。
> **【繁中】** 素材本身不可販售、不可再散布。

實務上：
- 一份衛教單用 3–8 張，遠在額度內，不用擔心
- **「同一件作品」的意思是同一份文件**，不是同一次對話
- 治療所自己印給個案 → 一般不算商用；但**拿去賣的教材、收費課程的講義算**
- 不確定就跑 `license_check()`，它會把原文條款一起回給你

### ③ 不要把插圖當成臨床素材

這些是可愛風格的通用插圖。可以用來**降低衛教單的壓迫感**，
不能用來代表特定診斷、特定個案，也不要拿來做情緒辨識或評估的題目。

---

## 為什麼需要這支工具：日文那道牆

いらすとや 是日文站，圖也是日文標的。使用者想找「深呼吸」，
站上叫「深呼吸をする人のイラスト」；想找「情緒」，站上是「感情」。

`search()` 內建 167 組中日對照詞，你打中文它自己翻成日文去查。
使用者不必知道這件事，**也不要在回答裡炫耀這個過程**，直接給圖就好。

真的要用日文原詞精準查，才用 `search_ja()`。

---

## 常見問法 → 該用哪支工具

| 使用者說 | 用這支 |
|---|---|
| 「找幾張放鬆的圖」「有沒有深呼吸的插圖」 | `search("放鬆")` |
| 「用日文原詞查」「查 ストレス」 | `search_ja("ストレス")` |
| 「有哪些主題可以挑」 | `list_topics()` |
| 「這個分類還有什麼」 | `browse_label(...)` |
| 「我要做一份給個案的衛教單，配幾張圖」 | `suggest_for_worksheet(...)` |
| 「可以印出來發嗎」「授權怎麼算」 | `license_check()` |
| **「做成 Artifact」「做成網頁」** | 先做 HTML → **`inline_images()`** → 才發布 |
| 「這個圖庫有多大」「有幾張」 | `library_stats()` |
| 「所有標籤列一下」 | `list_labels()` |
| 「給我一份可以直接印的 A4」 | `worksheet_template(...)` |

---

## 接到單元 3（做衛教單）時

使用者要的通常是「**一張紙，直接印出來給個案帶走**」。

1. 用 `worksheet_template()` 起版，它已經處理好 A4 尺寸與列印邊界
2. 配圖 3–8 張，用 `suggest_for_worksheet()` 挑，別自己硬湊
3. **發布前跑 `inline_images()`**（見硬規矩①）
4. 手機上也要能讀 —— 模板已經是 `width:min(210mm, 100%)`，不要自己改死寬度

> ⚠️ Artifact 的沙箱會擋掉頁面自己觸發的 `window.print()`，
> 所以模板裡放的是「**按 Ctrl+P 列印**」的常駐提示，不是列印按鈕。
> 不要好心幫他加一顆按鈕上去，那顆按鈕按了不會有反應。

搭配 `kxmind-tainan-mental-mcp` 可以在單張下半部附上真實的在地資源。
**那半部的電話與機構名一律來自那支工具的回傳，不准自己寫。**

---

## 這支不做的事

- 不下載整個圖庫、不做批次抓取。一次查幾張就好，這是別人的免費站。
- 不改圖、不去背、不合成。要編輯請使用者自己用繪圖軟體。
- 不保證某個關鍵字一定有圖。查不到就說查不到，換個詞再試，不要生一個假的圖網址。
